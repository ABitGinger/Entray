# 更新日志

本文件记录所有值得用户注意的改动。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.0.0] - 2026-09-19

全面翻新。核心目标只有一个：**让它在别人电脑上也能正常打开**。

### 修复

- **解决"打包后别人电脑无法使用"**：改为静态链接 MSVC 运行时库（`/MT`），不再依赖
  `VCRUNTIME140.dll` / `MSVCP140.dll`。此前发布的还是 Debug 版本，还额外依赖只在开发机上
  存在的 `ucrtbased.dll`，这是问题的最主要原因。
- **图标终于显示出来了**：原来的 `resources/resource.rc` 从未参与编译，而且它引用的
  `icon.ico` 其实是一张改名的 PNG，本来就编不过。现在图标、清单、版本信息都会内嵌进 exe。
- **补上应用程序清单**：显式声明 asInvoker，避免被 Windows 的安装程序探测启发式误判；
  声明高 DPI 感知，高分屏下托盘图标不再发虚。
- 开机自启路径现在会加引号。此前装在含空格的路径下（例如 `C:\Program Files\...`）时自启会失效。
- 自启路径会在启动时自动修正，exe 被挪走以后不必重新设置。
- 清除选择时只删除对应的注册表值，不再把整个键删掉重建。
- 目标程序文件丢失时不再静默清空配置，改为给出提示并允许重新选择。
- 不再对共享图标调用 `DestroyIcon`（原先销毁的是系统全局图标对象）。
- 单击托盘不会再启动两次（双击时 Explorer 会连发两条消息）。
- Explorer 重启后托盘图标会自动重新注册，不会再永久消失。
- 修复了源码里一处因编码混乱产生的乱码注释。

### 新增

- **关闭所选程序**：右键菜单新增。先向目标的顶层窗口发送 `WM_CLOSE` 请求正常退出，
  等待 2.5 秒仍未退出时，才询问用户是否强制结束。进程识别优先按可执行文件完整路径匹配，
  无法确定时宁可报"未找到"，也不会误杀。
- 托盘气泡通知：首次运行的引导、选择成功、目标文件丢失、已关闭等状态都有反馈。
- 单实例保护：重复双击不会出现第二个托盘图标，而是提示"已经在运行"。
- 右键菜单显示当前目标的名称与运行状态，"启动/关闭"项会据此自动置灰。
- "打开所在文件夹"菜单项，可在资源管理器里定位到目标程序。
- "关于"对话框，内含使用说明与配置位置。

### 变更

- 源码由单个 `main.cpp` 拆分为 `app` / `config` / `process` / `util` 四个模块，便于维护。
- 重新设计图标：绿白配色的多尺寸 `.ico`（16 / 20 / 24 / 32 / 40 / 48 / 64 / 128 / 256），
  每个尺寸单独绘制笔画粗细，小尺寸下依然清晰。原先的图标实为微信 logo，已替换。
- 托盘提示文字、菜单文案统一改为中文并说明更清楚。
- 构建系统重写：提供 CMake Presets、一键构建脚本 `scripts/build.ps1`、CPack 打包。
- 新增 CI：每次推送自动编译并校验产物，打 tag 时自动生成 Release。
- 新增 `tools/check_exe.py`，直接解析 PE 导入表来断言 exe 不依赖外部运行库，防止问题回归。

### 兼容性

- 支持 Windows 7 / 8 / 8.1 / 10 / 11（x64）。
- 配置格式与 v1.x 兼容：原有的 `HKCU\Software\Entray\TargetPath` 会被直接沿用，升级后无需重新选择。

## [1.2] - 2024-12-16

- 托盘图标自动更换为所选程序的图标。

## [1.1] - 2024-12-14

- 增加开机自启开关。

## [1.0] - 2024-12-14

- 首个版本：托盘图标、左键启动、右键菜单选择程序与退出。
