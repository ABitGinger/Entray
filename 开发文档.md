# Entray 开发文档

> 面向维护者的说明。只想用的话看 [README.md](README.md) 就够了。

## 1. 项目定位

一个常驻系统托盘的启动器：把用户选定的程序做成一个托盘图标，单击启动、右键管理。

设计上刻意守住三条线：

1. **单文件**。发布物只有一个 `Entray.exe`，双击即用，不写注册表以外的地方，不落地任何运行时文件。
2. **零依赖**。不依赖 .NET、不依赖 VC++ 运行库、不依赖任何 DLL。这是 v2.0.0 的重点修复项，见第 3 节。
3. **不越权**。所有配置写在 `HKEY_CURRENT_USER` 下，全程 asInvoker，任何时候都不会弹 UAC。

## 2. 架构

```
                    ┌──────────────────────────────┐
   wWinMain ───────▶│  main.cpp                    │
   (入口)           │  单实例互斥体 / CoInitialize │
                    └──────────────┬───────────────┘
                                   │
                    ┌──────────────▼───────────────┐
                    │  TrayApp (app.cpp)           │
                    │  隐藏顶层窗口 + 消息循环      │
                    │  托盘图标 / 右键菜单 / 气泡   │
                    └───┬──────────┬──────────┬────┘
                        │          │          │
             ┌──────────▼──┐  ┌────▼─────┐  ┌─▼──────────┐
             │ config.cpp  │  │process.cp│  │ util.cpp   │
             │ 注册表持久化 │  │启动/关闭 │  │ 字符串/路径 │
             └─────────────┘  └──────────┘  └────────────┘
```

### 各模块职责

| 文件 | 职责 |
| --- | --- |
| `main.cpp` | 进程入口。单实例互斥体、COM 初始化，然后交给 `TrayApp`。 |
| `app.cpp/.h` | 托盘应用本体。窗口注册、托盘图标生命周期、右键菜单、气泡通知、各条命令的实现，以及跟 `config` / `process` 的编排。 |
| `process.cpp/.h` | 与"目标程序"有关的一切：解析 `.lnk`、启动、按路径或文件名查找进程、优雅关闭与强制结束。 |
| `config.cpp/.h` | 注册表读写：目标路径、开机自启项的增删与自愈。 |
| `util.cpp/.h` | 无状态的工具：大小写无关比较、路径归一化、文件名/目录名提取、错误码翻译、调试日志。 |
| `common.h` | 全局常量：窗口类名、互斥体名、注册表位置、自定义消息 ID、超时时间。 |
| `version.h` | 版本号唯一来源，C++ 与 `resources/app.rc` 共用。 |

### 为什么是一个隐藏的顶层窗口，而不是 `HWND_MESSAGE`

`TaskbarCreated` 是 Explorer 重启后发出的**广播消息**，而消息窗口（`HWND_MESSAGE`）收不到广播。托盘程序必须能收到它，否则 Explorer 一重启图标就永远消失了。所以这里创建的是一个普通的顶层窗口，只是不显示。

## 3. 打包：为什么以前"别人电脑上打不开"

这是 v1.x 最严重的问题，值得单独记一笔。

**根因有两条，叠加在一起：**

1. **动态链接 CRT**。CMake 在 MSVC 下默认使用 `/MD`，生成的 exe 会导入 `VCRUNTIME140.dll`、`MSVCP140.dll`、`api-ms-win-crt-*.dll`。这些 DLL 只在装过 Visual Studio 或 VC++ 运行库的机器上才有。开发机当然有，于是"我这儿好好的"，拷到别人电脑上就报"找不到 VCRUNTIME140.dll"。
   —— 有意思的是 `开发文档.md` 里早就写着"静态链接运行时库"，但 CMakeLists 从头到尾没实现这一点。
2. **发布的是 Debug 版本**。当时的构建目录里 `CMAKE_BUILD_TYPE=Debug`，产物依赖 `ucrtbased.dll` / `vcruntimed.dll`。这两个 Debug 版运行库按设计就不允许再分发，几乎只存在于开发机上。

**另外还有两个连带问题：**

3. **图标根本没编进去**。`resources/resource.rc` 从未出现在 `add_executable()` 里，本来就是段死代码；而且它引用的 `resources/icon.ico` 实际上是一张改名的 PNG（微信 logo），rc 编译器看到会直接报"does not contain icon data"。结果就是 exe 没有图标，托盘一直用系统默认图标 —— 这也是它看起来糙的原因。
4. **没有应用程序清单**。没有清单的 exe 会被 Windows 的"安装程序探测启发式"扫一遍，可能莫名弹 UAC；同时因为没有声明 DPI 感知，托盘图标在高分屏上会被拉伸得发虚。

**v2.0.0 的对策：**

| 问题 | 修法 | 位置 |
| --- | --- | --- |
| 动态 CRT | `CMAKE_MSVC_RUNTIME_LIBRARY = MultiThreaded`（即 `/MT`） | `CMakeLists.txt` |
| Debug 版本 | 单配置生成器默认兜底成 `Release`；CI 只出 Release | `CMakeLists.txt`、`.github/workflows/build.yml` |
| 图标没编进去 | 真正的多尺寸 `.ico`，并由 `resources/app.rc` 引入目标 | `resources/`、`tools/make_icon.py` |
| 无清单 | `app.rc` 内嵌 `app.manifest`，声明 asInvoker / DPI 感知 / v6 控件 | `resources/app.manifest` |
| 防回归 | `tools/check_exe.py` 解析 PE，断言没有运行库依赖、资源齐全；CI 每次构建都跑 | `tools/check_exe.py` |

`tools/check_exe.py` 是这件事的关键：它不靠人工肉眼看构建日志，而是直接读 PE 的导入表来断言"exe 只依赖系统 DLL"。以后不管谁改了编译选项，只要把依赖改回动态链接，CI 立刻失败。

## 4. 持久化

全部在 `HKEY_CURRENT_USER` 下，不需要管理员权限：

| 位置 | 内容 |
| --- | --- |
| `HKCU\Software\Entray` → `TargetPath` (REG_SZ) | 用户选择的原始路径，可能是 `.lnk` |
| `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` → `Entray` (REG_SZ) | 开机自启命令 |

几个刻意的取舍：

- **只删值，不删键**。v1 的做法是先 `RegDeleteKey` 再重建，会把同键下的其他设置一起抹掉，现在改成 `RegDeleteValue`。
- **自启命令带引号**。`"C:\Program Files\...\Entray.exe"`。不带引号的话，路径里有空格时 Windows 会把第一个空格当成参数分隔符，自启直接失效 —— v1 就有这个 bug。
- **自启路径自愈**。每次启动时比对 `Run` 里的路径与当前 exe 的真实路径，不一致就改写。用户把 exe 从"下载"挪到别处之后，自启依然有效。
- **文件丢了不清配置**。v1 发现目标文件不存在就静默清空配置，用户下次启动会一脸茫然。现在保留路径，托盘提示"找不到文件"，菜单项标成"（文件已丢失）"，并提供重新选择。

## 5. "关闭所选程序"是怎么实现的

在托盘程序里"关掉别人"，难点在于**认准是哪一个进程**，宁可找不到，也不能误杀。

```
右键 → 关闭 XXX
  │
  ├─ 1. 定位目标进程
  │     先按「解析后的 exe 完整路径」精确匹配（对 .lnk 会先用 IShellLink 解析到真实 exe）
  │     匹配不到时，退回按「可执行文件名」匹配，并排除 C:\Windows 下的同名进程
  │     再补上「本程序亲自启动过的那个 PID」
  │
  ├─ 2. 一个都没匹配到 → 气泡提示"当前没有在运行"，结束
  │
  ├─ 3. 优雅关闭：给该进程的所有顶层窗口 PostMessage(WM_CLOSE)
  │     然后最多轮询等待 2.5 秒
  │     （轮询而不是 WaitForMultipleObjects，避开它 64 个句柄的上限）
  │
  ├─ 4. 全部退出了 → 不发通知（只写调试日志），结束
  │
  └─ 5. 还有活着的 → 弹框告知，"是否强制结束？未保存的数据会丢失"，默认按钮是「否」
        用户点「是」才 TerminateProcess
```

设计意图：

- **先礼后兵**。`WM_CLOSE` 是"请你退出"，目标程序能正常保存数据、清理自己的托盘图标。直接 `TerminateProcess` 会丢用户数据，所以只在用户明确确认后才做。
- **默认不误伤**。完整路径优先，文件名匹配是兜底且排除了系统目录；强杀必须用户点确认，且默认按钮是「否」。
- **关于"识别到多个进程"**：QQ、微信、Chrome 这类多进程程序会一次关掉一整套，这是预期行为。

## 6. 构建与打包

```powershell
# 一键：编译 + 自检 + 输出 dist\Entray.exe
powershell -ExecutionPolicy Bypass -File scripts\build.ps1

# 或者用 CMake Presets
cmake --preset msvc                 # 配置
cmake --build --preset msvc-release # 编译 → build\Release\Entray.exe
cpack --preset msvc-release         # 打包 → Entray-2.0.0-win64.zip

# MinGW 也能编（会静态链接 libgcc/libstdc++，产物同样是单文件）
cmake --preset mingw
cmake --build --preset mingw-release
```

版本号只有一处要改：`src/version.h`。`resources/app.rc` 直接 `#include` 它，CMake 的 `project(... VERSION)` 单独维护一份，改版本时两处一起改。

### 不要写死生成器版本

配置时**一律不要**带 `-G "Visual Studio 1x 20xx"`，`CMakePresets.json` 里的 `msvc` 预设也刻意不写 `generator` 字段——让 CMake 自己挑本机装的那个 Visual Studio。

原因是真踩过一次：工作流里原本写死 `-G "Visual Studio 17 2022"`，而 GitHub 的 `windows-latest` 镜像后来把 Visual Studio 升到了 **18（VS 2026）**，于是配置阶段直接报

```
CMake Error at CMakeLists.txt:3 (project):
  Generator "Visual Studio 17 2022" could not find any instance of Visual Studio.
```

整个流水线在头 13 秒就挂掉，而且因为编译器根本没被调用，看现象很像"代码编译不过"，其实是环境变了。生成器名字里带版本号，就等于把流水线的寿命绑在 runner 镜像的升级节奏上。

同理，CI 里也不要断言 runner 上装的是哪个版本的 VS。要排查这类问题，见第 11 节。

## 7. 发布流程

1. 改 `src/version.h` 和 `CMakeLists.txt` 里的 `project(... VERSION ...)`
2. 更新 `CHANGELOG.md`，提交
3. 合并到 `main`，打标签：`git tag -a v2.0.0 -m "Entray 2.0.0"`，推送标签
4. GitHub Actions 自动编译、跑 `check_exe.py` 自检、把 `Entray.exe` 和 zip 附到 Release

**自检没过就不要发布。** 那说明 exe 又变回依赖运行库的状态了。

## 8. 代码约定

- **只编 Unicode**。统一用 `-W` 后缀的 API（`MessageBoxW`、`Shell_NotifyIconW`…），不用 `TCHAR` 宏。
- **不用 `MAX_PATH` 缓冲区**。路径一律 `std::wstring` + 动态 `std::vector<wchar_t>`，配合清单里的 `longPathAware`。
- **源码是 UTF-8 无 BOM**。MSVC 靠 `/utf-8` 识别，MinGW 默认就按 UTF-8 读；`resources/app.rc` 额外用 `#pragma code_page(65001)` 并在 MinGW 下由 `--codepage=65001` 兜底。
- **只用 Windows 7 起就有的 API**。通过 `_WIN32_WINNT=0x0601` 在编译期卡死，避免无意间抬高系统要求。
- **资源句柄自己管好**。`TrayApp` 只拥有两个 `HICON`（`m_appIcon`、`m_targetIcon`），`m_trayData.hIcon` 永远只是指向其中一个，不做独立释放，从结构上避免重复 `DestroyIcon`。（v1 对 `LoadIcon(NULL, IDI_APPLICATION)` 返回的共享图标调用了 `DestroyIcon`，那是在销毁系统全局对象。）
- **调试输出走 `util::DebugLog`**，用 DebugView 就能看到，Release 下也保留（无调试器时几乎零开销）。

### 通知策略

界面上"弹一下"分三档，判断标准只有一条：**这条信息是不是用户做出下一个动作所必需的**。

| 形态 | 什么时候用 | 什么时候不用 |
| --- | --- | --- |
| 模态对话框 | 出错（启动失败、文件丢失、自启被安全软件拦下），以及有破坏性后果的确认（强制结束） | 一切正常路径 |
| 托盘气泡 | 首次运行的启动引导、"找不到目标程序"、"点了启动但目标没在运行" | 操作成功回执 |
| 静默 | **操作成功回执**：已选择、已清除、已关闭、已强制结束、已开启/关闭自启 | — |

- **成功回执一律不发气泡**。这些动作的结果在界面上本来就看得见——托盘图标和提示文字、菜单里的勾选项、菜单项根据运行状态自动置灰——再弹一次纯属噪音。需要留痕就写 `util::DebugLog`。
- 唯一保留的"结果类"气泡是"XXX 当前没有在运行"。它对应的是**用户以为做了某事、实际没做**，属于认知落差，值得打断一次。
- 以后加功能请沿用这套标准：**默认静默**，只有用户需要据此决定下一步时才弹。

## 9. 提交规范

用 [Conventional Commits](https://www.conventionalcommits.org/)：

```
<type>(<scope>): <描述>

feat     新功能          feat(app): 新增“关闭所选程序”
fix      修 bug          fix(config): 开机自启路径带空格时失效
build    构建/依赖        build: 改为静态链接 MSVC 运行库
ci       持续集成        ci: 增加产物单文件校验
docs     文档            docs: 补写打包问题根因分析
refactor 重构（不改行为） refactor(app): 拆分 TrayApp 的菜单构建
chore    杂项            chore: 添加 .gitignore
```

分支模型：`main` 保持随时可发布，改动走 `feature/*` / `fix/*` 分支，合并回来用 `--no-ff` 保留合并点。

## 10. 已知限制与后续想法

- 只能记忆**一个**目标程序。真要放多个图标，需要把配置从单值改成列表，并给每个目标分配独立 `uID` + 独立图标 —— `NOTIFYICONDATA.uID` 已经预留好这个扩展点。
- 没有代码签名，所以首次运行一定会有 SmartScreen 提示。要彻底解决只能买证书。
- 只支持 x64 与 x86，没有 ARM64 构建。代码本身没有架构相关假设，加一个 CI job 就能出。
- 还没做多语言。目前所有界面文案都在 `app.cpp` 里，抽一层 `strings.cpp` 就能支持。

## 11. CI 挂了但读不到日志时怎么办

Actions 的日志要登录才能下载，本地脚本又拿不到 token。这时不要靠猜，**让 CI 把要查的东西主动交出来**：

在 `.github/workflows/` 下临时加一个工作流（`diag.yml`），跑完把收集到的环境信息和命令输出**发成一个 Issue**——Issue 正文是可以通过 API 直接读的，这就绕开了日志权限：

```yaml
permissions:
  contents: read
  issues: write      # 发 Issue 需要
steps:
  - name: 收集环境与配置输出
    shell: pwsh
    continue-on-error: true    # 这一步挂了也要继续，否则发不出结果
    run: |
      $ErrorActionPreference = 'Continue'   # 关键：别让原生命令的 stderr 直接把这一步顶掉
      ...
      (& cmake -S . -B ci-build -A x64 2>&1 | Out-String) | Out-File ci-diag/result.txt
  - name: 把结果发成 Issue
    if: always()
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    run: |
      Invoke-RestMethod -Method Post -Uri 'https://api.github.com/repos/<owner>/<repo>/issues' `
        -Headers @{ Authorization = "Bearer $env:GH_TOKEN" } -Body (...) -ContentType 'application/json'
```

两个要点：

- `$ErrorActionPreference = 'Continue'`。GitHub 的 pwsh 默认是 `Stop`，原生命令只要往 stderr 写一行东西就会被当成异常抛出，看起来像命令失败，其实不是。
- 收集步骤要 `continue-on-error: true`，发送步骤要 `if: always()`，否则收集一失败就什么都拿不到。

查完记得把这个临时工作流删掉，别留在仓库里。
